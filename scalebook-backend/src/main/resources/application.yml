spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scalebook_db
    username: postgres
    password: Gauri@post6
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
    cache:
      type: redis